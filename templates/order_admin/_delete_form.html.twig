<link href="/button.css" rel="stylesheet">
<form method="post" action="{{ path('app_order_admin_delete', {'id': order.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ order.id) }}">
    <button class="retro-button">Delete</button>
</form>
